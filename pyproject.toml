[project]
name = "tabcaddy"
version = "0.1.6"
description = "Dataset-centric CLI toolkit for exploring, compiling, transforming, and diffing tabular data"
readme = "README.md"
requires-python = ">=3.11"

authors = [
  {name = "Matthias Lenga" }
]

license = "Apache-2.0"
license-files = ["LICENSE"]

dependencies = [
    "asciichartpy>=1.5.25",
    "numpy>=2.4.6",
    "polars>=1.41.2",
    "pyarrow>=24.0.0",
    "pydantic>=2.13.4",
    "rich>=15.0.0",
    "tqdm>=4.67.1",
    "tzdata>=2025.2",
    "typer>=0.26.7",
    "benchcaddy>=0.1.13",
]

[project.scripts]
tabcaddy = "tabcaddy.cli.app:main"

[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pre-commit>=4.2.0",
    "pytest>=9.0.3",
]
